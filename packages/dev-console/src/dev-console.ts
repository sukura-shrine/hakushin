// import { registerMicroApps, start } from 'qiankun'
// import { getClientPackagesInfo } from './utils'

async function main () {
  // const apps = await getClientPackagesInfo('./')
  // const list = apps.map(item => {
  //   const { name, hakushin } = item
  //   return {
  //     name,
  //     activeRule: name,
  //     container: '#dev-console',
  //     entry: `//localhost:${hakushin.port}` }
  // })
  // registerMicroApps(list)
  // start()
}

main()
